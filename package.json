{
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@package/esbuild": "^1.0.0",
    "@package/tailwindcss": "^1.0.0"
  },
  "scripts": {
    "build": "NODE_ENV=production npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve --quiet"
  },
  "workspaces": [
    "packages/*"
  ]
}